/** returns a list of books with the given generateKey.
* @param {string} genre - The genre of books required.
*@returns {Book[]} An array of Book objects matching the specified genre.
*/
function filterByGenre(genre, books) { }

    var returnValue = {
        //Die nächste Aktion
        nextStep: "showText",
        //Daten für die nächste Aktion
        text: "Hello World"
    }
    //Daten zurückgeben
    return JSON.stringify(returnValue);